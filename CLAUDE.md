# 아덴의 그림자 — 프로젝트 규칙

브라우저 싱글플레이 RPG. React + Vite + Tailwind + vitest.
정적 웹(GitHub Pages), 서버·DB·로그인 없음.

**이 파일에는 사고 원칙만 있다.** 영역별 규칙과 기계가 지키는 것은 따로 있다.

| 무엇 | 어디 |
|---|---|
| 그리는 쪽 — 좌표 검증 · 그림자 · 앵커 · 빛 | `.claude/rules/rendering.md` |
| 그림 파일 — 팔레트 · 윤곽선 · trim · 프레임 정렬 | `.claude/rules/sprites.md` |
| 시험 · 타입 · 빌드가 성한지 | **훅이 끝낼 때 막는다** (`.claude/hooks/verify.sh`) |
| core · content · balance 를 지시 없이 못 고치게 | **훅이 막는다** (`.claude/hooks/guard-core.sh`) |

---

## 0. 이 게임이 무엇을 만들려 하는가 — 모든 판단의 기준

> 게임을 켰을 때 "오늘 나는 이 세계에서 뭘 해볼까" 라는 생각이 들 것.
> 재미 = 결과를 모르는 선택을 하고, 그 결과를 감당하는 것.

세 조건이 모두 있어야 한다. 하나라도 빠지면 재미가 아니라 작업이다.
1. 선택지가 둘 이상이고 서로 포기하는 것이 있다
2. 결과가 불확실하다
3. 되돌릴 수 없다

새 기능은 반드시 이 셋에 대본다. 못 채우면 가짓수만 늘리는 것이다.
편의는 선택을 지우고 무게는 선택을 만든다.
다만 무엇이 안 되는지 알 수 없게 만드는 것은 무게가 아니라 결함이다.
잃는 것은 분명해야 하고, 왜 잃었는지는 항상 보여야 한다.

게임 전체의 모양은 @docs/전체설계.md 를 따른다.
**만드는 순서는 그 문서 10절 하나뿐이다.** 다른 문서가 순서를 말하면
그 위에 무엇을 얹는지를 말하는 것이지 순서를 다시 정하는 것이 아니다.

- 전체설계.md — 게임 전체가 어떤 모양인가. 크기, 축, 상한, 지역 구조, **순서**
- 목적지-기획안.md — 무엇을 향해 가는가. 세 축(①②③)과 세 갈래
- 이야기-기획안.md — 그 위에 무엇을 얹는가. 실버우드, 두린, 발도르
- 사운드-설계.md — 타격감. **코드가 이 문서를 절 번호로 참조한다**
- 밟은-지뢰.md — 무엇이 어떻게 고장났었나. 낡지 않는 기록
- 그림-작업.md — 그림을 이미지로 바꾸는 일의 실측과 결정

---

## 1. 어떻게 판단하는가 — 세 가지

이 셋은 실제로 틀렸던 자리에서 나왔다. 지키지 않으면 같은 데서 또 틀린다.

### 1.1 추측보다 코드의 실제 상태를 먼저 본다

「아마 이럴 것이다」로 시작하지 마라. **읽어서 확인하고 시작한다.**

- 값을 말하기 전에 그 값이 어느 파일 몇 번째 줄에 있는지 찾는다
- 「모양이 셋이니 셋 다 같은 식일 것」이라고 넘겨짚었다가 틀렸다 —
  박쥐만 `anim` 이 아니라 `world.time` 으로 돌고 있었다
- 화면에 무엇이 어디 있는지는 **세계에서 받는다.** 픽셀로 되짚지 마라
  (`.claude/rules/rendering.md` 1절)
- 확인 못 한 것은 **「확인 못 했다」고 적는다.** 빈칸을 그럴듯한 값으로 채우지 마라

### 1.2 전제가 검증되기 전에 그 위에 계산을 쌓지 않는다

전제가 틀리면 그 위의 표는 전부 쓰레기다. **전제를 먼저 재고, 다르면 멈추고 말한다.**

- 「2π 가 한 순환」이라는 전제로 일곱 종의 표를 만들었는데,
  그중 한 종은 애초에 그 값을 안 쓰고 있었다. 표 한 칸이 통째로 뜻이 없었다
- 「도형에서 rate 를 고치면 미끄러짐이 준다」는 전제로 후보를 넷 재려다,
  **도형에는 딛는 발 자체가 없다**는 것을 뒤늦게 알았다
- 남의 API 도 마찬가지다 — 한쪽 출력이 다른 쪽 입력에 그대로 들어간다고 가정하지 마라

### 1.3 같은 접근이 두 번 실패하면 방법을 바꾼다

세 번째로 같은 것을 시도하지 마라. **접근을 바꾸거나 멈추고 묻는다.**

- 화면 픽셀로 사람을 찾다가 세 번 틀렸다. 네 번째가 아니라 다른 길이 답이었다
- 같은 시험이 세 번 연속 실패하면 즉시 멈춘다. 원인 분석과 대안만 보고하고 기다린다.
  **반쯤 고친 코드를 덧대지 마라.** 고치려던 것을 되돌릴지는 그때 정한다 —
  같은 판에서 한 다른 멀쩡한 일까지 날리는 것이 더 큰 손해일 수 있다

---

## 2. 조용한 실패 금지

반환값을 무시하지 마라. `??` 기본값으로 조회 실패를 덮지 마라.
데이터가 있으면 화면에 보여라. 표의 키는 실제 id 와 맞아야 한다.
**안 되는 일은 왜 안 되는지 화면에 나와야 한다.**

숫자 하나가 조용히 0 이 되는 것이 제일 무섭다 —
`rgb()` 문자열을 hex 로 읽어 `NaN → 0` 이 되면서 짐승이 전부 새까맣게 나왔는데
예외도 로그도 없었다. **못 읽으면 던져라.**

---

## 3. 버그 재현은 씨앗으로

씨앗 + 상황으로 재현한다. 재현이 안 되면 고치지 말고 먼저 보고한다.
고친 뒤 그 재현 케이스를 테스트로 남긴다.

---

## 4. 깨지 말 것 — 아키텍처 불변조건

1. 계산 가능한 값을 저장하지 않는다 (derive)
2. 지형을 저장하지 않는다 (씨앗 생성 + 도달성 검사)
3. `ui/` 는 게임 규칙을 판단하지 않는다 (`.claude/rules/rendering.md` 4절)
4. content(표)와 logic(계산)을 분리한다
5. seeded RNG 를 유지한다
6. `core/` 는 `audio/` 와 `dev/` 를 모른다

---

## 5. 기술 제약

정적 웹 · 서버 없음 · **의존성 추가 금지** · localStorage
· UI 는 폰 세로가 기준, 데스크톱은 그 위에서 넓게 쓴다

의존성 금지는 그대로다. PNG 를 읽고 쓸 일이 생겼을 때 `tools/png.mjs` 를
직접 짠 것이 이 규칙을 지킨 것이고, `@types/node` 대신 `tests/node-fs.d.ts` 에
쓰는 둘만 적은 것도 같은 이유다. 앞으로도 그렇게 한다.

### 이미지 asset — 금지에서 조건부 허용으로

**왜 고쳤나: `public/props/` 에 그림 열 장이 실제로 들어갔는데 규칙은 아직
이미지를 금지하고 있었다. 규칙이 저장소를 못 따라온 것이라 규칙을 맞춘다.**

허용하되 넷을 지킨다.

1. **`public/` 아래 정적 이미지만.** 런타임에 만들지 않고 외부에 요청하지 않는다.
   빌드가 그대로 복사하는 파일이어야 한다
2. **없으면 도형으로 떨어진다.** 폴백이 필수다 — 그림 한 장을 빼도 화면이
   비지 않고 예전 도형이 나와야 한다. 그래서 한 장씩 켜고 끌 수 있다
3. **로딩 실패를 조용히 삼키지 마라** (2장). `drawImage` 에 아직 안 받아진
   이미지를 넣으면 브라우저가 예외도 로그도 없이 아무것도 안 그린다.
   `sprites.ts` 의 `spriteFor` 가 그것을 막는 장치다
4. **4절 불변조건은 그대로다.** 씨앗으로 만들고 지형을 저장하지 않는다.
   **이미지는 그리는 것이지 세계를 정하는 것이 아니다** — 어느 칸에 무엇이
   서는지는 여전히 씨앗이 정하고, 그림은 그 결과를 칠할 뿐이다

자세한 규약은 `.claude/rules/sprites.md`.

---

## 6. 끝내기 전에 — 훅이 막지 않는 것들

시험 · 타입 · 빌드는 **훅이 본다.** 여기 적지 않는다.
훅이 못 보는 것만 남긴다.

1. **봇은 조건부다**
   - `core/` · `content/` · `balance.ts` 를 건드렸으면 `tools/sim.ts` 를 돌리고 지표 보고
   - `ui/` · `audio/` · `dev/` 만 고쳤으면 돌리지 않는다
   - 봇은 시간이 오래 걸리는 수치를 재고 회귀를 확인하는 도구다.
     "어느 쪽이 나은가", "재미있는가" 같은 선택과 감각은 봇에게 묻지 않는다.
     그것은 사람이 판단한다. **봇이 어떤 선택을 했는지를 밸런스 근거로 쓰지 마라**
   - 돌리는 법:
     ```
     npx esbuild tools/sim.ts --bundle --format=esm --platform=node --outfile=/tmp/sim.mjs
     node /tmp/sim.mjs <시작자리> <시간> <초점> [trace] [씨앗]
       시작자리 miner|smith|guard · 초점 balanced|mine|fight
     ```
2. `git diff --stat` 보고
3. 배포하고 URL 을 알려준다
4. 브라우저에서 실제 플레이 가능

**중간 상태로 두지 않는다.** 끝내지 못하면 되돌리고 보고한다.

---

## 7. 요청하지 않은 변경 금지

- 요청하지 않은 리팩터링·개명·파일이동·포매팅을 하지 않는다
- 관련 없는 문제를 발견하면 고치지 말고 보고만 한다
- 지시받은 파일 밖으로 나가야 하면 먼저 묻는다

`core/` · `content/` · `balance.ts` 는 **훅이 막는다.** 여기 적지 않는다.

---

## 8. 애매하면 묻는다

추측해서 진행하지 마라. 물어보는 비용보다 되돌리는 비용이 훨씬 크다.
